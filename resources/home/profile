if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi

alias app='cd /data/CHANGEME/current'
alias sc='cd /data/CHANGEME/current && script/console'
alias shared='cd /data/CHANGEME/shared'

export RAILS_ENV='<%= rails_env %>'
